project.name = pin
project.default_charset = utf-8
server.index = 8383
server.search = 8384

[pin_id]
type = id

[pin_type]
index = self
tokenizer = full

[user_id]
index = self
tokenizer = full

[title]
type = title

[desc]
type = string
index = both
phrase = yes

[content]
type = body

[ctime]
type = numeric

[status]
type = numeric
index = self
tokenizer = full
